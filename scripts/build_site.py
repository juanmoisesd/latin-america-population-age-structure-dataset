import pandas as pd, os, re, itertools
from pathlib import Path
# Este script se entrega como referencia. La versión funcional ya fue usada para generar el sitio en /docs.
print("Sitio ya generado. Sustituye dataset_copy.csv por tu CSV actualizado y adapta la lógica si añades más dimensiones.")
